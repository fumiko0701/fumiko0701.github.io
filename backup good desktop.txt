<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Próximo Culto</title>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --background-color-light: #f0f0f0;
            --card-background-color-light: #ffffff;
            --text-color-light: #333;
            --accent-color-light: #4CAF50;

            --background-color-dark: #1a1a1a;
            --card-background-color-dark: #2e2e2e;
            --text-color-dark: #e0e0e0;
            --accent-color-dark: #45a049;
        }

        body {
            margin: 0;
            padding: 0;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            background-color: var(--background-color-light);
            font-family: 'Roboto', sans-serif;
            transition: background-color 0.3s ease;
        }

        .card {
            display: flex;
            background-color: var(--card-background-color-light);
            padding: 0; /* Remove padding do card para evitar espaço */
            box-shadow: 0 6px 15px rgba(0, 0, 0, 0.1);
            border-radius: 15px;
            width: 90%;
            max-width: 1200px;
            height: auto;
            box-sizing: border-box;
            position: relative;
            overflow: hidden; /* Oculta bordas que possam aparecer */
        }

        .card img {
            width: 45%;
            height: auto;
            border-radius: 15px 0 0 15px; /* Bordas arredondadas à esquerda */
            border-top-right-radius: 15px; /* Adiciona borda arredondada no canto superior direito */
            border-bottom-right-radius: 15px; /* Adiciona borda arredondada no canto inferior direito */
            object-fit: cover;
            object-position: center; /* Centraliza a imagem dentro do espaço */
            aspect-ratio: 16 / 9; /* Define a razão de aspecto da imagem */
            margin-left: -20px; /* Move a imagem para fora do card */
            margin-right: 30px; /* Espaço entre a imagem e o conteúdo da direita */
            position: relative; /* Permite que a imagem fique no topo do card */
            z-index: 1; /* Garante que a imagem fique acima do card */
            margin-top: -20px; /* Remove espaço no topo */
            margin-bottom: -20px; /* Remove espaço na parte inferior */
        }

        .card-content {
            flex: 1;
            display: flex;
            justify-content: center; /* Centraliza o conteúdo na div */
            align-items: center; /* Centraliza o conteúdo na div */
            padding: 20px; /* Espaçamento entre o conteúdo e as bordas da div */
        }

        #nomeCulto {
            font-size: 3vw;
            font-weight: 700;
            color: var(--text-color-light);
            white-space: nowrap;
            overflow: hidden;
        }

        #descricaoCulto {
            font-size: 2vw;
            color: gray;
            margin-top: 5px;
        }

        .fields {
            width: 100%;
            display: flex;
            flex-direction: column; /* Coloca os campos em coluna */
            align-items: flex-start; /* Alinha os campos à esquerda */
        }

        .fields p {
            font-size: 1.8vw;
            margin: 5px 0;
            color: var(--text-color-light);
        }

        .emoji {
            font-size: 2vw;
        }

        @media (min-width: 768px) {
            #nomeCulto {
                font-size: 24px;
            }

            #descricaoCulto {
                font-size: 16px;
            }

            .fields p {
                font-size: 18px;
            }

            .emoji {
                font-size: 24px;
            }
        }

        /* Botões */
        .floating-btn {
            position: fixed;
            bottom: 20px;
            right: 20px;
            width: auto;
            height: auto;
            padding: 10px 20px;
            color: white;
            border-radius: 30px; /* Bordas arredondadas para botão retangular */
            border: none;
            display: flex;
            justify-content: center;
            align-items: center;
            cursor: pointer;
            box-shadow: 0 6px 10px rgba(0, 0, 0, 0.2);
            transition: all 0.3s ease;
            font-size: 16px;
            text-align: center;
        }

        .floating-btn-copy {
            background-color: #3498db;
            margin-bottom: 70px;
        }

        .floating-btn-copy:hover {
            background-color: #2980b9;
        }

        .floating-btn-send {
            background-color: #27ae60;
        }

        .floating-btn-send:hover {
            background-color: #229954;
        }

        /* Botão de alternar tema */
        .theme-toggle-btn {
            position: fixed;
            top: 20px;
            left: 20px;
            background-color: #ddd;
            color: #333;
            border: none;
            padding: 10px;
            border-radius: 50%;
            cursor: pointer;
            font-size: 20px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            transition: background-color 0.3s ease, color 0.3s ease;
        }

        .theme-toggle-btn:hover {
            background-color: #ccc;
        }

        /* Tema escuro */
        body.dark-mode {
            background-color: var(--background-color-dark);
        }

        body.dark-mode .card {
            background-color: var(--card-background-color-dark);
            box-shadow: 0 6px 15px rgba(0, 0, 0, 0.5);
        }

        body.dark-mode #nomeCulto,
        body.dark-mode .fields p {
            color: var(--text-color-dark);
        }
    </style>
</head>
<body onload="loadTheme()">
    <!-- Botão de alternar tema -->
    <button class="theme-toggle-btn" onclick="toggleTheme()">🌗</button>

    <div class="card">
        <img src="ceia.jpg" alt="Próximo Culto">
        <div class="card-content">
            <div class="fields">
                <p id="nomeCulto"><strong></strong></p>
                <p id="descricaoCulto"></p>
                <p><span class="emoji">💻</span> - <span id="pc"></span></p>
                <p><span class="emoji">🎥</span> - <span id="cam1"></span></p>
                <p><span class="emoji">🎥</span> - <span id="cam2"></span></p>
                <p><span class="emoji">🤳</span> - <span id="storys"></span></p>
                <p><span class="emoji">🎤</span> - <span id="reporter"></span></p>
                <p><span class="emoji">📷</span> - <span id="fotos"></span></p>
            </div>
        </div>
    </div>

    <!-- Botões -->
    <button class="floating-btn floating-btn-copy" onclick="window.location.href='escala-online.html'">Visualizar escala</button>
    <button class="floating-btn floating-btn-send" onclick="copyText()">Copiar</button>

    <script>
        // Definindo variáveis com o conteúdo de cada campo
        const nomeCulto = "SANTA CEIA - 19h";
        const descricaoCulto = "Sexta-feira | 04/10";
        const pc = "ISAAC";
        const cam1 = "SAMUEL S";
        const cam2 = "SAMUEL F";
        const storys = "DAIRANE";
        const reporter = "";
        const fotos = "INGRID";

        // Atribuindo os valores ao HTML
        document.getElementById('nomeCulto').innerHTML = `<strong>${nomeCulto}</strong>`;
        document.getElementById('descricaoCulto').textContent = descricaoCulto;
        document.getElementById('pc').textContent = pc;
        document.getElementById('cam1').textContent = cam1;
        document.getElementById('cam2').textContent = cam2;
        document.getElementById('storys').textContent = storys;
        document.getElementById('reporter').textContent = reporter;
        document.getElementById('fotos').textContent = fotos;


        // Função para copiar o texto formatado para a área de transferência
        function copyText() {
            const formattedText = `*${nomeCulto}*\n________________________________\n\n💻 - ${pc}\n🎥 - ${cam1}\n🎥 - ${cam2}\n🤳 - ${storys}\n🎤 - ${reporter}\n📷 - ${fotos}`;
            navigator.clipboard.writeText(formattedText).then(() => {
                alert("Texto copiado para a área de transferência!");
            }).catch(err => {
                console.error('Erro ao copiar: ', err);
            });
        }

        function toggleTheme() {
            document.body.classList.toggle('dark-mode');
            const currentTheme = localStorage.getItem('theme');
            if (currentTheme === 'dark') {
                localStorage.setItem('theme', 'light');
            } else {
                localStorage.setItem('theme', 'dark');
            }
        }

        function loadTheme() {
            const savedTheme = localStorage.getItem('theme');
            if (savedTheme === 'dark') {
                document.body.classList.add('dark-mode');
            }
        }
    </script>
</body>
</html>
